query ($folderId: String) {
  smtasks {
    tasks(folderId: $folderId) {
      id
      name
      descript
      addedFio
      addedPhoto
      dateplan
      status
      isMy
      myControl
      isGenerate
      hasOrig
      hasComm
      priority
      dateFact
      isRead
      caseId
      isFavorite
      docCaption
    }
  }
}
