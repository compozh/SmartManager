mutation ($id: Int!, $type: String!, $attachments: String!) {
  smtasksMutation {
    addAttachments(id: $id, type: $type, attachments: $attachments) {
      id,
      success,
      errorMessage
    }
  }
}
