mutation($formCode: String!, $params: FormCustomEventParamsInput!) {
    formioQueryMutation {
        callFormCustomEvent(formCode: $formCode, params: $params) {
            components
            submission
            display
            settings
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}