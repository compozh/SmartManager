query($formCode:String!, $properties: String, $deviceSizeType: String) {
    formioQuery {
        getForm(formCode: $formCode, properties: $properties, deviceSizeType: $deviceSizeType) {
            name
            components
            submission
            display
            settings
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}