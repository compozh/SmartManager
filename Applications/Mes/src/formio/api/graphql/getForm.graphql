query($formCode: String!, $params: FormGettingParamsInput!) {
    formioQuery {
        getForm(formCode: $formCode, params: $params) {
            name
            components
            submission
            display
            settings
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}