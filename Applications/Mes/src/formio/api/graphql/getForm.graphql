query($formCode:String!, $properties: DictionaryType!) {
    formio {
        getForm(formCode: $formCode, properties: $properties) {
            components
            submission
            display
            settings
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}