query($formCode:String!, , $properties: String) {
    formioQuery {
        getForm(formCode: $formCode, properties: $properties) {
            name
            components
            submission
            display
            settings
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}