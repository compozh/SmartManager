mutation($formCode: String!, $submission: String, $properties: String) {
    formioQueryMutation {
        submitForm(formCode: $formCode, submission: $submission, properties: $properties) {
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}