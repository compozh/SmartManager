mutation($formCode: String!, $submission: String, $properties: DictionaryType!) {
    formioMutation {
        submitForm(formCode: $formCode, submission: $submission, properties: $properties) {
            components
            submission
            display
            settings
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}