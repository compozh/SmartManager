mutation($formCode: String!, $params: FormSubmitParamsInput!) {
    formioQueryMutation {
        submitForm(formCode: $formCode, params: $params) {
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}