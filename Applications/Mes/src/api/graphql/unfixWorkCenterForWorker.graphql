mutation($fixationId:Int!) {
    mesMutation {
        unfixWorkCenterForWorker(fixationId: $fixationId) {
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}