query($formCode: String!, $properties: DowntimeParamsInput!){
    mes{
        downtimeFormio(formCode: $formCode, properties: $properties) {
            name
            components
            submission
            display
            settings
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}