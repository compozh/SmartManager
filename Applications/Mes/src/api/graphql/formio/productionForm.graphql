query($formCode: String!, $params: ProductionRegistrationParamsInput!){
    mes{
        productionFormio(formCode: $formCode, params: $params) {
            name
            components
            submission
            display
            settings
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}