mutation($formCode:String!, $params: String!) {
    mesMutation {
        productionFormioSubmit(formCode: $formCode, params: $params) {
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}