mutation($formCode:String!, $submission: String, $properties: ProductionRegistrationParamsInput!) {
    mesMutation {
        productionFormioSubmit(formCode: $formCode, submission: $submission, properties: $properties) {
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}