mutation($formCode:String!, $params: FormSubmitParamsInput!) {
    mesMutation {
        productionFormioSubmit(formCode: $formCode, params: $params) {
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}