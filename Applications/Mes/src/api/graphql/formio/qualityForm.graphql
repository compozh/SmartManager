query($formCode: String!, $properties: ProcessParamsInput!){
    mes{
        getFormio(formCode: $formCode, properties: $properties) {
            name
            components
            submission
            display
            settings
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}