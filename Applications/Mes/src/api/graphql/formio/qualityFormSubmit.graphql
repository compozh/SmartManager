mutation($formCode:String!, $submission: String, $properties: ProcessParamsInput!) {
    mesMutation {
        qualityFormioSubmit(formCode: $formCode, submission: $submission, properties: $properties) {
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}