mutation($userId: ID, $lessonGuid: ID, $parentPostId: ID, $post: String){
  lmsMutation{
  	addPost(userId: $userId, lessonGuid: $lessonGuid, parentPostId: $parentPostId, post: $post){
      success
      successMessage
      errorCode
      errorMessage
      postId
    }  
  }
}