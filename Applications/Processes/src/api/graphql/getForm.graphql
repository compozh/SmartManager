query($processDefinitionId: String!) {
    processesQuery {
        getForm(processDefinitionId: $processDefinitionId) {
            formKey
            form {
                name
                components
                submission
                display
                settings
                success
                errorMessage
                successMessage
                errorCode
            }
            success
            errorMessage
            errorCode
            statusCode
        }
    }
}