mutation($processParams: String!) {
    processQueryMutation {
        startProcess(formCode: $processParams) {
            processInstance {
                id
            }
            success
            errorMessage
            errorCode
            statusCode
        }
    }
}