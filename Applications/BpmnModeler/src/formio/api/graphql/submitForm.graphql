mutation($formCode: String!, $submission: String) {
    formioQueryMutation {
        submitForm(formCode: $formCode, submission: $submission) {
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}