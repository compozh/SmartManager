query($formCode:String!, $properties: String) {
    formioQuery {
        getForm(formCode: $formCode, properties: $properties) {
            name
            components
            submission
            display
            settings
            isSystem
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}