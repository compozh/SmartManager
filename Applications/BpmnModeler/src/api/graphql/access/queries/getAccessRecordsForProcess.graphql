{
  bpmnquery {
    getAccessRecordsForProcess(processId: $processId) {
      allowAccess,
      groupId,
      groupName,
      recordId,
      rights,
      type,
      userId,
      userName
    }
  }
}