{
  bpmnquery {
    getAccessRecordsForDiagram(diagramId: $diagramId) {
      id,
      recordId,
      groupId,
      groupName,
      userId,
      userName,
      type,
      read,
      write,
      deploy,
      execute,
      share,
      parentId,
      inherit
    }
  }
}