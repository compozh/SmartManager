{
  bpmnquery {
    getAccessRecordsForDiagram(diagramId: $diagramId) {
      allowAccess,
      groupId,
      groupName,
      recordId,
      rights,
      type,
      userId,
      userName
    }
  }
}