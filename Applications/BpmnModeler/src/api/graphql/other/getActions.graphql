{
  bpmnquery {
    getAvailableActions(definitionType: $definitionType, onlySystem: $onlySystem) {
      code,
      name,
      formCode,
      isSystem,
      categories {
        id,
        name,
        icon
      }
    }
  }
}
