mutation($taskId:Int!) {
    mesMutation {
        cancelBeginRegistration(taskId: $taskId) {
            success
            errorMessage
            successMessage
            errorCode
        }
    }
}