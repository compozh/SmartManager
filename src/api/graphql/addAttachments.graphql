mutation ($attachments: String!, $params: String) {
  smtasksMutation {
    addAttachments(attachments: $attachments, params: $params) {
      id
      name
      success
      errorMessage
    }
  }
}
